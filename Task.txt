1. liquibase миграция
2. весь проект
3. стороннее апи
4. пагинация + фильтрация
5. вынести конфиг для postgresql(при старте приложения запускается из .env файла(должна запущена быть в docker DB PG))
6. swagger для ручек(комментариями надо делать)